#version 330 core

in vec2 passTexCoord;
in vec3 passNormal;

uniform sampler2D modelTexture;
out vec4 fragColor;

void main() {

    vec4 texturecolour = texture(modelTexture,passTexCoord);
    fragColor = vec4(1.0) * texturecolour; 
}
