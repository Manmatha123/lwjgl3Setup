#version 330 core

in float vDistance;
out vec4 out_Color;

uniform vec3 color;
uniform float dashSize;
uniform float gapSize;

void main() {
    float total = dashSize + gapSize;

    if (mod(vDistance, total) > dashSize) {
        discard;
    }

    out_Color = vec4(color, 1.0);
}
